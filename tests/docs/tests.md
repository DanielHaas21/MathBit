# MathBit - `tests`

This package contains the test suites for MathBit, including unit tests and end-to-end tests.
These tests test the following functionalities:

- Web App functionality and UI components
- CRUD API endpoints and integration
- Haskell backend logic and performance (through web-api, not the raw Haskell code)
